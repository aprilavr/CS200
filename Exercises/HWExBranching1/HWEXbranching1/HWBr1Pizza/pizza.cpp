#include <iostream>
#include <string>
using namespace std;
//if cout<<likesPizza, returns 0 for F, 1 for T
int main()
{
    bool likesPizza = false;

    if (likesPizza == true)
    {
        cout<< "Likes pizza." << endl;
    }
    else
    {
        cout<< "Doesn't like pizza." << endl;
    }

    while (true) {} //temp-keeps running
    return 0;
}
